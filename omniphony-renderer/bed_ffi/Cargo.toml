[package]
name = "bed_ffi"
version = "0.1.0"
edition = "2024"
license = "GPL-3.0-or-later"
description = "Realtime authored multichannel-bed ABI for the native Omniphony SFX"

[lib]
name = "omniphony_bed"
crate-type = ["cdylib", "rlib"]

[dependencies]
orender_engine = { path = "../orender_engine" }
